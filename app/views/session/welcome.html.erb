<h1>ponq</h1>

<% if logged_in? %>
  Welcome, <%= current_user.first_name %>
  <%= link_to "Logout", logout_url, method: :post %>
<% else %>
  <%= link_to login_url, method: :get do %>
    <div class="primary-button">Login</div>
  <% end %>
  <p>-OR-</p>
  <%= link_to new_user_url, method: :get do %>
    <div class="secondary-button">Create Account</div>
  <% end %>
<% end %>
