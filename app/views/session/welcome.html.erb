<h1>ponq</h1>

<% if logged_in? %>
  Welcome, <%= current_user.first_name %>
  <%= button_to "Logout", root_url, method: :get %>
<% else %>
  <%= button_to "Login", login_url, method: :get %>
  <%= button_to "Create Account", new_user_url, method: :get %>
<% end %>
